server {
    listen              80;
    server_name         localhost;
    charset             utf-8;

    location / {
        include         /etc/nginx/proxy_params;
        proxy_pass      http://unix:/run/amanda.sock;
    }

    location /static/ {
        alias           /srv/amanda/.static/;
    }
}